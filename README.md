# rrhh-frontend

Frontend Next.js 14 con NextAuth (Cognito + Google federado) y consumo del gateway en `http://localhost:8078`.

## Scripts

```bash
npm install
npm run dev
npm run test
npm run build
```

## Variables

Copiar `.env.example` a `.env.local` y completar Cognito + `NEXTAUTH_SECRET`.
